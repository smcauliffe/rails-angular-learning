<h1>New User</h1>

<%= form_for :user, url: users_path do |f| %>
  <% if @user.errors.any? %>
    Error
  <% end %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>